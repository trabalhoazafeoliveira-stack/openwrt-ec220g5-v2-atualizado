name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # EXEMPLO (pode remover depois)
      - name: Teste
        run: echo "Build iniciado"

      # ⚠️ AQUI entraria o build REAL do OpenWrt
      # Exemplo:
      # - name: Build OpenWrt
      #   run: |
      #     make defconfig
      #     make -j$(nproc)

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware-openwrt
          path: bin/targets/**/*
